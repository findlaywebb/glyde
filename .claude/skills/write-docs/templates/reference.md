# <Component / API / command> reference

<!-- MODE: reference — information-oriented. Mirror the product's structure, not a journey. Describe, don't instruct or explain. Exhaustive + consistent. -->

<One-sentence description of what this is.>

## <Item name / signature>

<One or two precise sentences: what it is and does.>

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `<name>`  | `<type>` | yes/no | `<default>` | <meaning> |

**Returns:** `<type>` — <meaning>

**Errors:**
- `<error>` — <condition>

**Example:**
```
<minimal correct snippet>
```

## <Next item>

<!-- repeat the same block structure for every item — consistency is the readability -->
