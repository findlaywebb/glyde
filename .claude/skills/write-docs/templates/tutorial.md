# Tutorial: <Build / get X working>

<!-- MODE: tutorial — learning-oriented. The reader is new and must succeed. Show, don't explain. One path, no options. Link the "why" out to an explanation doc. -->

By the end of this you'll have <the concrete thing they'll have built/seen>.

## Before you start

You need:
- <exact prerequisite, with version>
- <install command, copy-pasteable>

## Step 1 — <action>

<Exact command or action.>

```
<runnable code — no placeholders in the load-bearing line>
```

You should see:

```
<expected output — so they can self-check>
```

## Step 2 — <action>

…

## You're done

You now have <the working result>. <One line confirming the win.>

## Where next

- To do <task>, see the how-to: <link>
- To understand *why* this works, see: <explanation link>
